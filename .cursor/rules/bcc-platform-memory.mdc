---
description: BCC hosting, n8n automation, and CRM decisions from project planning
alwaysApply: true
---

# BCC platform memory

## Product
- Burmese Catholic Community of Western Australia dynamic website + staff Admin Panel.
- GitHub: `https://github.com/kyawzinIT99/BCC-website.git`

## Hosting split (do not change without owner confirmation)
- **Hostinger Pro**: domain, SSL, up to 10 mailboxes (planned; not in API portfolio yet).
- **Existing Hostinger KVM 2 VPS** (`srv1707349.hstgr.cloud`, IP `76.13.194.176`, Ubuntu 24.04 + Docker/Traefik): currently runs n8n (`n8n-al8a.srv1707349.hstgr.cloud`). Keep AI/CRM automation here. Do **not** buy another VPS just for n8n.
- **Coming Linux VPS / website essentials**: deploy BCC website (app, DB, media, reverse proxy, backups) — either on a new VPS or as additional Docker services on this KVM 2 if capacity allows. Prefer not duplicating n8n.
- Hostinger API connected via `HOSTINGER_API_TOKEN` in `.dev.vars` (never commit). Base: `https://developers.hostinger.com`.

## Recently built n8n workflows (already integrated)
- `BCC Inquiry Alert` (`U1gvYq4WnZvxyMIm`) → `/webhook/bcc-inquiry-alert` (created + status changed + closed; closed includes staff `handledBy` / `closedBy`)
- `BCC Publish Distribution` (`czx6JcuNWP2unBRv`) → `/webhook/bcc-publish-distribution`
- `BCC Subscribe Alert` (`JBDK9TqPnYJCkAMn`) → `/webhook/bcc-subscribe-alert` (new/reactivated subscribers → Telegram/Gmail staff alert)
- `BCC Event Mail` (`bS9NmXubTc9y9Kc0`) → `/webhook/bcc-event-mail` (published upcoming event → Gmail to active subscribers + staff Telegram summary)
- Staff Telegram alerts use HTML parse mode with a shared layout: `BCC · …` headline, bold name, escaped user text (safer than Markdown for names/emails).
- Website env in `.dev.vars`: `CRM_ALERTS_ENABLED`, `N8N_INQUIRY_ALERT_WEBHOOK`, `N8N_PUBLISH_WEBHOOK`, `N8N_SUBSCRIBE_ALERT_WEBHOOK`, `N8N_EVENT_MAIL_WEBHOOK`, shared `N8N_INQUIRY_WEBHOOK_SECRET` (never commit).

## Mail subscribe → Admin → n8n → event emails
- Public form on Home + Events (`MailSubscribe`) posts to `/api/subscribers` with consent.
- Admin panel **Subscribers** list (owner/admin only); can mark unsubscribed.
- New subscribe alerts staff via `N8N_SUBSCRIBE_ALERT_WEBHOOK`.
- Publishing an **upcoming** event calls `N8N_EVENT_MAIL_WEBHOOK` with event details + active subscriber emails for Gmail delivery in n8n.
- Draft events never trigger mail. Re-saving an already-published event does not re-send.

## Enquiry close alerts + customer report
- Inquiry PATCH status changes call the same inquiry webhook (`community.inquiry.closed` / `community.inquiry.status_changed`) with signed-in staff `displayName` as `handledBy`.
- Closing sets `closed_by` / `closed_at` on `public_inquiries`; reopening clears them.
- Admin **Reports** (owner/admin): `/api/reports/customers` combines enquiries + subscribers with filters; Excel export (`.xlsx`) uses separate sheets **Enquiries** and **Subscribers**; optional PDF export keeps the same two sections on separate pages.

## Pending owner inputs
- Hostinger Pro domain purchase/attach + mailbox setup when ready.
- Decide website deploy target: new Linux VPS vs Docker services on existing KVM 2.

## Editorial / reputation
- Draft → review → publish → then n8n only. Never send drafts to automation.
- No government/endorsement claims without written evidence.
